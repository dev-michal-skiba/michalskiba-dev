#!/bin/bash

docker compose --file test.docker-compose.yml run auth_test bash -c \
  'DB_PATH=src/sql_injection/auth.db ALLOW_ORIGIN=http://localhost:1313 python -m pytest tests/ &&
  python -m coverage report'

