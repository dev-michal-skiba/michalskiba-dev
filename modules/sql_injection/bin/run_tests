#!/bin/bash

docker compose --file test.docker-compose.yml run sql_injection_test bash -c \
  'DB_PATH=src/sql_injection/sql_injection.db ALLOW_ORIGIN=http://localhost:1313 python -m pytest tests/ &&
  python -m coverage report'

