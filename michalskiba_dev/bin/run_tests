#!/bin/sh
RED='\033[0;31m'
GREEN='\033[0;32m'
NOCOLOR='\033[0m'

test_directory="./"
if [ $# -eq 1 ]
then
  test_directory=$1
fi

if ! pytest
then
  echo "${RED}Unit tests check failed${NOCOLOR}"
  exit 1
fi
echo "${GREEN}Unit tests check succeeded${NOCOLOR}"

if ! coverage report --show-missing --fail-under=90
then
  echo "${RED}Coverage check failed${NOCOLOR}"
  exit 1
fi
echo "${GREEN}Coverage check succeeded${NOCOLOR}"

echo
echo "${GREEN}== Test checks passed ==${NOCOLOR}"